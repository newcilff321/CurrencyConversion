spring:
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/hw
    username: user1
    password: user1
  # Jpa
  jpa:
    database: mysql
    show-sql: true
    hibernate:
      ddl-auto: none
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
server:
  servlet:
    encoding:
      force: true